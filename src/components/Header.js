import React, { Component } from 'react';
import { PageHeader } from 'react-bootstrap';

class Header extends Component {
  render() {
    return (
      <PageHeader className="text-center">Resumen del monto recibido</PageHeader>
    )
  }
}

export default Header;
